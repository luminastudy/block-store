import { describe, it, expect } from 'vitest'
import { selectLoading } from './selectLoading.js'

describe('selectLoading', () => {
  it('should be defined', () => {
    expect(selectLoading).toBeDefined()
  })
})

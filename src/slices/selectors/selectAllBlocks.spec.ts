import { describe, it, expect } from 'vitest'
import { selectAllBlocks } from './selectAllBlocks.js'

describe('selectAllBlocks', () => {
  it('should be defined', () => {
    expect(selectAllBlocks).toBeDefined()
  })
})

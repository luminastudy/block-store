import { describe, it, expect } from 'vitest'
import { selectSourcesByProvider } from './selectSourcesByProvider.js'

describe('selectSourcesByProvider', () => {
  it('should be defined', () => {
    expect(selectSourcesByProvider).toBeDefined()
  })
})

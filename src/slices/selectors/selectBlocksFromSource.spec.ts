import { describe, it, expect } from 'vitest'
import { selectBlocksFromSource } from './selectBlocksFromSource.js'

describe('selectBlocksFromSource', () => {
  it('should be defined', () => {
    expect(selectBlocksFromSource).toBeDefined()
  })
})

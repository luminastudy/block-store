import { describe, it, expect } from 'vitest'
import { selectAllSources } from './selectAllSources.js'

describe('selectAllSources', () => {
  it('should be defined', () => {
    expect(selectAllSources).toBeDefined()
  })
})

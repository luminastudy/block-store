import { describe, it, expect } from 'vitest'
import { selectBlockById } from './selectBlockById.js'

describe('selectBlockById', () => {
  it('should be defined', () => {
    expect(selectBlockById).toBeDefined()
  })
})

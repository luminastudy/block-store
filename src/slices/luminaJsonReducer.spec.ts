import { describe, it, expect } from 'vitest'
import luminaJsonReducer from './luminaJsonReducer.js'

describe('luminaJsonReducer', () => {
  it('should be defined', () => {
    expect(luminaJsonReducer).toBeDefined()
  })
})

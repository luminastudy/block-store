import { describe, it, expect } from 'vitest'
import { removeLuminaJson } from './removeLuminaJsonAction.js'

describe('removeLuminaJsonAction', () => {
  it('should be defined', () => {
    expect(removeLuminaJson).toBeDefined()
  })
})

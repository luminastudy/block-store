import { describe, it, expect } from 'vitest'
import { clearSources } from './clearSourcesAction.js'

describe('clearSourcesAction', () => {
  it('should be defined', () => {
    expect(clearSources).toBeDefined()
  })
})

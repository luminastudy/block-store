import { describe, it, expect } from 'vitest'
import { clearError } from './clearErrorAction.js'

describe('clearErrorAction', () => {
  it('should be defined', () => {
    expect(clearError).toBeDefined()
  })
})

import { describe, it, expect } from 'vitest'
import { addLuminaJson } from './addLuminaJson.js'

describe('addLuminaJson', () => {
  it('should be defined', () => {
    expect(addLuminaJson).toBeDefined()
  })
})
